pipeline {
         agent any
         
         stages {
                 stage('One') {
                 steps {
                          
                             
                             bat 'echo %GIT_BRANCH% >> git.txt'
                             set a= %GIT_COMMIT%
                            mkdir %a%       
                                   
                          
              
                 }
                 }
         }      
}
